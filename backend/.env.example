# SECRET_KEY — это случайная строка для криптографии Django.
# Используется для подписи сессий, CSRF-токенов и т.д.
# Сгенерировать можно командой: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Или онлайн: https://djecrety.ir/
SECRET_KEY=

# DEBUG=True для разработки, False для продакшена
DEBUG=True

# Домены/IP, с которых можно открыть сайт
# Для локальной разработки: localhost,127.0.0.1
# Для продакшена: your-domain.com,123.45.67.89
ALLOWED_HOSTS=localhost,127.0.0.1

# ===========================================
# База данных PostgreSQL
# ===========================================

# Пароль для PostgreSQL (любой, ты сам его придумываешь)
POSTGRES_PASSWORD=

# ===========================================
# ZenClass
# ===========================================

# Секретные ключи вебхуков хранятся в БД (по одному на курс).
# Управление: Django Admin → Курсы → выбрать курс → секрет вебхука (inline).
# Здесь ничего указывать не нужно.

# ===========================================
# Telegram Bot
# ===========================================

# Токен бота от @BotFather
# Получить: написать /newbot в @BotFather, следовать инструкциям
TELEGRAM_BOT_TOKEN=

# ===========================================
# Google Sheets (одноразовый импорт студентов)
# ===========================================

# ID таблицы — берётся из URL:
# https://docs.google.com/spreadsheets/d/ЭТОТ_ID_НУЖЕН/edit
GOOGLE_SHEETS_SPREADSHEET_ID=

# Путь к JSON-файлу с ключом сервисного аккаунта Google
# Как получить:
# 1. Открыть https://console.cloud.google.com/
# 2. Создать проект (или выбрать существующий)
# 3. APIs & Services → Enable APIs → включить "Google Sheets API"
# 4. APIs & Services → Credentials → Create Credentials → Service Account
# 5. Открыть созданный аккаунт → Keys → Add Key → Create new key → JSON
# 6. Скачать файл, положить в backend/credentials/
# 7. ВАЖНО: дать доступ к таблице этому сервисному аккаунту (его email вида xxx@project.iam.gserviceaccount.com)
GOOGLE_SERVICE_ACCOUNT_FILE=credentials/service-account.json
