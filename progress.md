# Прогресс изменений

## Сделано (закоммичено, нужен деплой)

### 1. Фикс "·" перед названием первого урока в расписании МОШ
- Файл: `backend/index.html`, строка 614
- Причина бага: код `{isToday ? '· ' : ''}{item.topic}` добавлял "·" в начало названия урока когда событие "сегодня"
- Исправление: убрали "· " из названия — информация "сегодня" уже есть в подписи ниже

### 2. Комментарий преподавателя в истории работ
- Файл: `backend/index.html`
- Что сделано: в модальном окне (при нажатии на задание в истории) теперь показывается блок "Комментарий" если он есть
- Данные берутся из поля `teacher_comment` модели Grade — уже приходят через API `/api/grades/`
- Маппинг в tasks: добавлен `comment: g.teacher_comment || ''`

## Нужно сделать вручную (требует SSH или Django Admin)

### 3. Студент goncarovserafim450@gmail.com не видит курс МОШ по истории
- Скорее всего куплен до того как была настроена автоматизация вебхуков в ZenClass
- Нужно: вручную зачислить через Admin → Core → Подписки на курсы → Добавить
  - Студент: goncarovserafim450@gmail.com
  - Курс: Интенсив к МОШ по истории 25/26
  - Статус: Активен

### 4. Деплой
```bash
ssh root@45.10.245.122
cd /opt/kruzhok/backend && git pull origin main && docker compose up -d --build
```
